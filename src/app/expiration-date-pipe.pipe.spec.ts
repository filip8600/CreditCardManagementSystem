import { ExpirationDatePipePipe } from './expiration-date-pipe.pipe';

describe('ExpirationDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ExpirationDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
